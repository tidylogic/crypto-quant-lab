# Custom FreqAI Models

Put custom `IFreqaiModel` implementations here when the built-in models are not enough.

Start with built-in models such as `LightGBMRegressor` before adding custom model code.

